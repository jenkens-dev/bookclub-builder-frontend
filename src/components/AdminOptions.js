import React from 'react';

const AdminOptions = () => {
   return (
      <div>
         <button>Create Poll</button>
         <button>Edit Club</button>
      </div>
   );
};

export default AdminOptions;
